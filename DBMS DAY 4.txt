

character functions
lower
select lower(first_name) from employees;
upper
select upper(first_name) from employees;
initcap
select initcap(first_name) from employees;

concat
select concat(first_name,last_name) from
employees;
substr
select first_name,substr(first_name,1,3)
from employess;

length
instr
trim
select trim('   ocl') from dual;
select trim('0' from '00045000') 
from dual;
select trim(leading '0' from '00045000')
from dual;
select trim(trailing '0' from '00045000')
from dual;


DATE FUNCTIONS
SYSDATE

select months_between('01-FEB-02','01-MAR-03') from dual;
select add_months('01-FEB-02',6) from dual;
select next_day('26-MAY-2026','FRIDAY') from dual;
select last_day('26-MAY-2026') from dual;








Specialized Functions for NULL
NVL (expr1,expr2)
NVL2 (expr1,expr2,expr3)
nullif(expr1,expr2)
select length(first_name),length(last_name)
nullif(length(first_name),length(last_name))
from employees;
coalesce(expr1,expr2,......exprn)











SELECT employee_id,first_name,
 salary,
CASE
WHEN salary >= 15000 THEN 'HIGH'
WHEN salary >= 8000 THEN 'MEDIUM'
ELSE 'LOW'
END AS salary_grade
FROM employees;











SELECT employee_id,first_name,
       department_id,
       DECODE(department_id,
        10, 'Administration',
         20, 'Marketing',
         30, 'Purchasing',
     'Other Department') AS 
dept_name
FROM employees;


		GROUP FUNCTIONS 
SUM
AVG
MIN
MAX
STDDEV
VARIENCE
COUNT
select avg(salary),sum(salary),min(salary)
max(salary) from employees;


ORDER BY--->ASC, DESC
select first_name from employees 
order by salary;

GROUP BY
select department_id,count(*) from employees
group by(department_id);

HAVING 
















